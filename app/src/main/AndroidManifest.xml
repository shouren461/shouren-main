<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--获取网络请求权限,写入文件权限，读取文件权限-->
    <uses-feature android:name="android.hardware.camera.any" /> <!--确保配备相机，可以是前置摄像头，也可以是后置摄像头-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- Android 13+ 照片权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android 11+ 包可见性配置 - 允许分享到其他应用 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND_MULTIPLE" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />   <!--打通可见性管道，声明我的应用有权限和日历应用沟通-->
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shouren">
        <activity
            android:name="com.example.shouren.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--注册两个二维码创建类-->
        <activity android:name="com.example.shouren.activity.YoutubeCreateActivity" />
        <activity android:name="com.example.shouren.activity.CalendarCreateActivity" />
        <activity android:name="com.example.shouren.activity.HistoryItemDetailActivity"/>
        <activity android:name="com.example.shouren.activity.ScanItemDetailActivity" />
        <!--添加FileProvider文件分享配置-->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:resource="@xml/filepaths"
                android:name="android.support.FILE_PROVIDER_PATHS"
                />
        </provider>

    </application>

</manifest>
